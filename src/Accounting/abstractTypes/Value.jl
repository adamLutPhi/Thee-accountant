```
a(n) Abstract `seed` of idea
that has value

value
is a value fixed (or changing?)
what changes in a value

definitely:
    Any `thing` : requires time & effort

    effort: could be the production material the `thing` is made of
```
struct Value <: idea

    time #estimated time
    effort
end
